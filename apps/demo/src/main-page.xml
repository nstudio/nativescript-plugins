<Page xmlns="http://schemas.nativescript.org/tns.xsd" navigatingTo="navigatingTo" class="page">
  <Page.actionBar>
    <ActionBar title="Plugin Demos" icon="" class="action-bar"/>
  </Page.actionBar>
  <StackLayout class="p-20">
    <ScrollView class="h-full">
      <StackLayout>
        <Button text="nativescript-checkbox" tap="{{ viewDemo }}" class="btn btn-primary view-demo"/>
        <Button text="nativescript-exoplayer" tap="{{ viewDemo }}" class="btn btn-primary view-demo"/>
        <Button text="nativescript-filterable-listpicker" tap="{{ viewDemo }}" class="btn btn-primary view-demo"/>
        <Button text="nativescript-input-mask" tap="{{ viewDemo }}" class="btn btn-primary view-demo"/>
        <Button text="nativescript-onfido" tap="{{ viewDemo }}" class="btn btn-primary view-demo"/>
        <Button text="nativescript-plaid" tap="{{ viewDemo }}" class="btn btn-primary view-demo"/>
      </StackLayout>
    </ScrollView>
  </StackLayout>
</Page>